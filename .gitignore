# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Archive (local backup of old files, not tracked)
Archive/

# Environment & secrets
.env
.env.local
.dev.vars
*.key
*.pem

# Node.js (for future scripts)
node_modules/
package-lock.json

# IDE
.vscode/
.idea/

# Build artifacts
out/
cache/
artifacts/
dist/
claim-site/worker/.wrangler/

# Foundry — ignore deps + local artifacts only.
# foundry.toml / remappings.txt / test/ / script/ are PUBLIC (credible repo needs them).
lib/
broadcast/
forge-cache/
.forge-snapshots/

# Claude internal
.claude/
CLAUDE.md

# Working backlog + engineering log (local only, not for public repo)
Forge_BACKLOG.md
Forge_engineering_log.md
sop/

# Private technical reference — Jason's own understanding base, NOT for public repo
website/PIF12_Contract_Architecture.html
